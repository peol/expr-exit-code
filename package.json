{
  "name": "expr-exit-code",
  "version": "0.1.0",
  "description": "Evalutes a CLI parameter expression and exits with exit codes based on the boolean outcome of that expression.",
  "main": "index.js",
  "bin": {
    "expr-exit-code": "dist/index"
  },
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "babel src/ -d dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Andrée Hansson <peolanha@gmail.com>",
  "license": "ISC",
  "dependencies": {
    "commander": "^2.9.0",
    "expression-parser": "^1.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-core": "^6.7.2",
    "babel-preset-es2015": "^6.6.0"
  }
}
